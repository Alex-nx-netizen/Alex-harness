## P-2026-4-30-001 加 progress.md 扫描 → 跨 session 模式发现（人手种子，灵感: Ralph）

**direction**: `cross_session_pattern_mining`
**source**: `external_inspiration / human_seeded`（非 evolution-tracker 自动生成；Ralph github.com/snarktank/ralph 的 progress.txt 启发）
**evidence_runs**: `[]`（无；本议案是预防性/前瞻性，不是从 run 数据回看）
**evidence_findings**: `[]`（无具体 F-NNN；潜在覆盖未来类似 F-014/F-015/F-016 的"自跑发现自身 bug"模式）

**What**:
在 `.claude/skills/evolution-tracker/SKILL.md` Phase 1 READ 的步骤 4 之后插入新一步——读 `_meta/progress.md` 最近 14 天 session entries，按关键词过滤（`踩坑 / 坑 / 失败 / abort / F- / 没生效 / 重复 / 自跑`）抽取段落，作为 Phase 2 ANALYZE 的第三个证据流（除 runs.jsonl + findings.md 之外）。每条引用必须落到具体 session 标题（如"会话 9 续 9"），不允许抽象概括。

**Why**:
Ralph 模式的关键发现是 `progress.txt`（追加式学习日志）作为跨 iteration 的"暗记忆"——很多模式在它里面被叙述但没被结构化提炼。我们 `_meta/progress.md` 已是这个角色（Claude 写、人审），目前 evolution-tracker 没消费。这会丢真实模式：F-014/F-015/F-016 都是"自跑暴露自身 bug"——progress.md 完整记录了"会话 9 第 3 次跑触发自身 dangling"的全过程，findings.md 只摘了片段。让 evolution-tracker 读 progress.md 才能跨 session 看到"自跑发现自身 bug"是反复模式而不是孤例。

**Where**: `.claude/skills/evolution-tracker/SKILL.md` Phase 1 第 4 步之后插入第 4.5 步；Phase 2 操作 1 增加"progress.md 段落"为聚类维度。

**Risk**:
1. **噪声暴增**：进度日志啰嗦，引入它会让 Phase 2 假议案率上升 → mitigate: 14 天窗口 + 关键词过滤双闸 + 强制每条引用落到具体 session 标题。
2. **凭空发明 direction**：progress.md 可能比 runs.jsonl 早暴露问题，但缺 evidence_runs 链接 → mitigate: 来自 progress.md 的议案必须额外打 `source: progress_md_only` 标签，3 振规则前先要求"补 1 条 valid run 实证"。
3. **与 context-curator 重复扫**：M3 #1 context-curator 也要扫 progress.md 最新 N 条 → mitigate: 两者目的不同（curator 喂会话开头摘要 / tracker 找跨 session 模式），但**都从 progress.md 读不修改**，无写冲突；窗口/过滤规则可在 M3 末统一审视。
4. **Ralph 模式自宣告完成的隐患**：Ralph 让 AI 自己写 progress.txt 自己读自己；我们是 Claude 写人审 → 风险被人审兜住，可控。

**Diff**:

```diff
--- a/.claude/skills/evolution-tracker/SKILL.md
+++ b/.claude/skills/evolution-tracker/SKILL.md
@@ Phase 1: READ @@
 **操作**：
 1. `node` parse `<skill>/logs/runs.jsonl` 每一行；任何 parse fail **立即 abort**（铁律 #3，不能在坏数据上分析）
 2. 读 `<skill>/logs/.evolution-cursor`（不存在则视为首次跑，cursor=null）
 3. 读 `<skill>/SKILL.md` 全文 + frontmatter
 4. 读项目 `_meta/findings.md`（取所有 F-NNN 引用）
+4.5. 读项目 `_meta/progress.md` 最近 14 天 session entries（按 `## YYYY-M-D` 标题切段）；
+     **关键词过滤**：仅保留含 `踩坑 / 坑 / 失败 / abort / F- / 没生效 / 重复 / 自跑 / dangling` 的段落；
+     每个保留段落必须能引用到具体 session 标题（如 "会话 9 续 9"），不允许抽象概括；
+     输出到 `logs/phase1-progress-<run_id>.log` 供 Phase 2 消费。
 5. 计算 `valid_run_count = count(runs where completed != false && user_feedback.rating != null)`
@@ Phase 2: ANALYZE @@
 **操作**：
-1. 跨 run 找共性（聚类维度：`errors[]` 关键词、`learnings[]` 主题、`fix_notes` 词频）
+1. 跨 run 找共性（聚类维度：`errors[]` 关键词、`learnings[]` 主题、`fix_notes` 词频、**Phase 1.4.5 的 progress.md 段落主题**）
 2. 每条 fix_notes / errors → 给一个 `direction`（关键词聚类 ID，如 `phase4_robustness` / `phase7_push` / `self_evolution` / `chunk_size_limit`）
+   - 来自 progress.md（无 evidence_runs）的议案：`source` 字段标 `progress_md_only`，3 振前需补 1 条 valid run 实证
 3. 对照 SKILL.md：
```

**进入 evolution-tracker 实跑前的人审决策**:
- ✅ approve → 把上面 diff 落到 SKILL.md，下次跑 evolution-tracker 自动消费 progress.md
- ⏸ defer → 等 W3 收集 progress.md 中确实有"未提炼到 findings"的真实例子 ≥ 3 条再 reconsider
- ❌ reject → 写 reject_reason，记 1/3 同 direction 计数

**Alex 备注（待你拍）**: 此议案是从 Ralph 灵感来的预防性议案，非 tracker run 自然产出。建议先 defer 到 W3 末复盘——届时 context-curator 已跑过几次会话，可以一并审视 progress.md 的消费策略是分摊给两个 skill（curator 摘要 / tracker 模式挖掘）还是统一到一个 reader 里。
